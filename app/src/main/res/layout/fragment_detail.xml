<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools">

    <data>
        <variable
                name="viewModel"
                type="com.jamlabsai.srj.detail.DetailViewModel"/>
    </data>
    <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">
        <uk.co.senab.photoview.PhotoView
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                tools:srcCompat="@tools:sample/backgrounds/scenic"
                android:id="@+id/photo_large"
                android:scaleType="centerCrop"
                app:photoViewImageUri="@{viewModel.selectedImagePath}"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"/>
        <com.google.android.material.floatingactionbutton.FloatingActionButton
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:id="@+id/share_fab"
                android:layout_gravity="end|bottom"
                android:layout_marginBottom="40dp"
                app:layout_constraintBottom_toBottomOf="@+id/photo_large"
                app:layout_constraintEnd_toEndOf="@+id/photo_large"
                android:layout_marginEnd="40dp"
                android:src="@drawable/ic_baseline_share_24px"
                android:contentDescription="@string/share"
                android:layout_margin="8dp"
        />
    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>